description = "Minimal fixture-tested Shell preset."

[[files]]
source = "hello.ts"
target = "shine-demo-hello"
runtime = "bun"
platforms = ["unix", "windows"]
env = ["DEMO_GREETING"]
description = "Print a configurable example greeting."

[files.permissions]
schema_version = 1
commands = ["bun"]
environment = [{ name = "DEMO_GREETING", sensitivity = "plain" }]
