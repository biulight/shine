name: ci

on:
  push:
    branches:
      - release
  pull_request:
    branches:
      - release
      - main

permissions:
  contents: read
  pull-requests: write

jobs:
  test:
    uses: ./.github/workflows/test.yml
