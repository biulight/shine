# shine-core

Shared, UI-agnostic helpers for the Shine CLI and future Shine applications.

The crate currently provides comment-preserving TOML table synchronization and
helpers for creating `shine.toml` preset metadata files.
