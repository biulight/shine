#!/bin/bash
# Verify that the shell tools installed by shine can be invoked correctly.
# Check whether commands such as `setproxy` and `usetproxy` are present in PATH.
# Usage: test_tools

echo "🔍 Testing proxy settings..."
